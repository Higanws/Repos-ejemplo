# Diagrama de arquitectura (Mermaid)

| Archivo | Descripción |
|---------|-------------|
| [`arquitectura_ingesta.mmd`](arquitectura_ingesta.mmd) | Flujo único: ingesta validada/rechazada, Glue, EventBridge y orquestación. |

**Visualización:** GitHub/GitLab (Markdown con bloque `mermaid`), VS Code con extensión Mermaid, o `npx @mermaid-js/mermaid-cli -i arquitectura_ingesta.mmd -o salida.svg`.
